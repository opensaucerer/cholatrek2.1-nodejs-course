// MORE ON GLOBALS

// __dirname -- provides the path to the current directory

// __filename -- provides the path to the current file

// module -- uses commonJS for importing and exporting nodejs files

// exports -- a nodeJS object that contains all exported nodeJS files

// process -- a nodeJS object that contains all about the current node environment

console.log(__dirname);

console.log(__filename);

console.log(module);

console.log(module.exports);

console.log(process);
